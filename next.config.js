/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'https://my-portfolio-next-js-pi.vercel.app/'
        ]
    },
}

module.exports = nextConfig
